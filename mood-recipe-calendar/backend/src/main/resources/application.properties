spring.application.name=mood-recipe-backend
server.port=8080

# ========== MySQL 数据源 ==========
spring.datasource.url=jdbc:mysql://localhost:3306/mood_recipe?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== JPA ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ========== 文件上传 ==========
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
upload.dir=./uploads
# 本地为空；部署到 CDN/对象存储回源域名时设置，如 https://cdn.example.com
upload.public-base-url=${UPLOAD_PUBLIC_BASE_URL:}
# 腾讯云 COS：生产环境仅通过环境变量提供密钥；bucket 形如 mood-recipe-1250000000。
tencent.cos.enabled=${TENCENT_COS_ENABLED:false}
tencent.cos.secret-id=${TENCENT_COS_SECRET_ID:}
tencent.cos.secret-key=${TENCENT_COS_SECRET_KEY:}
tencent.cos.region=${TENCENT_COS_REGION:}
tencent.cos.bucket=${TENCENT_COS_BUCKET:}
tencent.cos.prefix=${TENCENT_COS_PREFIX:uploads/}

# ========== 微信小程序配置 ==========
# 配置真实 AppID/AppSecret 后，小程序 code 走微信 code2session；
# code 带 h5_dev_ 前缀仍走 mock（H5 本地开发调试）
# 仅在部署环境设置；不要把真实 AppID/AppSecret 提交到仓库。
wechat.appid=${WECHAT_APPID:}
wechat.secret=${WECHAT_SECRET:}

# ========== 微信虚拟支付（个人小程序）==========
# 仅在部署环境中设置下列变量，绝不能提交 OfferID/AppKey 或 session_key 加密密钥。
# 商品道具 ID 在 virtual_products.platform_item_id 中维护，必须与后台道具管理一致。
wechat.virtual-payment.offer-id=${WECHAT_VIRTUAL_PAYMENT_OFFER_ID:}
wechat.virtual-payment.app-key=${WECHAT_VIRTUAL_PAYMENT_APP_KEY:}
wechat.virtual-payment.environment=${WECHAT_VIRTUAL_PAYMENT_ENV:0}
# 微信小程序后台「消息推送」中配置的 Token；用于校验异步发货通知。
wechat.message-push.token=${WECHAT_MESSAGE_PUSH_TOKEN:}
wechat.message-push.encoding-aes-key=${WECHAT_MESSAGE_PUSH_ENCODING_AES_KEY:}
# Base64 编码的 32 字节随机密钥，用于在数据库中加密 session_key。
wechat.session-key-encryption-key=${WECHAT_SESSION_KEY_ENCRYPTION_KEY:}
security.session.days=${SESSION_DAYS:30}

# ========== Jackson ==========
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=Asia/Shanghai

# ========== AI 菜谱推荐（OpenAI 兼容接口）==========
# 不要把密钥写进此文件。运行前设置环境变量：AI_RECIPE_API_KEY、AI_RECIPE_MODEL。
# 任何兼容 Chat Completions 的服务均可通过 AI_RECIPE_BASE_URL 覆盖默认地址。
ai.recipe.api-key=${AI_RECIPE_API_KEY:}
ai.recipe.base-url=${AI_RECIPE_BASE_URL:https://api.openai.com/v1/chat/completions}
ai.recipe.model=${AI_RECIPE_MODEL:}
