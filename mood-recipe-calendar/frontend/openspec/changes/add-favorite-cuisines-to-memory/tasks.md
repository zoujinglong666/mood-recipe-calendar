## 1. 偏好数据契约

- [x] 1.1 在后端用户偏好实体和偏好接口中增加可为空的 `favoriteCuisines` 字段，并覆盖长度校验、保存与读取
- [x] 1.2 在前端偏好类型和请求参数中加入 `favoriteCuisines`，兼容旧接口缺少该字段的情况

## 2. 锅仔记忆交互

- [ ] 2.1 在锅仔记忆页加入常见菜系多选区，加载时回显并在保存、跳过和清除时同步处理
- [ ] 2.2 检查菜系选择在 H5 与微信小程序的触控尺寸、选择状态、深色模式和无选择状态

## 3. 个性化消费

- [ ] 3.1 在现有推荐评分中加入菜系关键词命中权重，并让推荐理由能说明锅仔记得该菜系偏好
- [ ] 3.2 将菜系偏好加入 AI 深度推荐上下文和锅仔寄语上下文及后备文案

## 4. 验证

- [ ] 4.1 增加后端测试，覆盖菜系保存、推荐命中加权和无匹配回退
- [ ] 4.2 运行后端测试、前端 ESLint、类型检查、H5 与微信小程序构建
- [ ] 4.3 运行 `openspec validate add-favorite-cuisines-to-memory --strict` 并核对规格与实现一致
