## 1. 周计划数据与生成服务

- [ ] 1.1 创建周计划、计划日和购物项数据模型、仓储及鉴权接口，保证用户只能访问自己的计划。
- [ ] 1.2 定义生成请求与结构化 AI 输出契约，带入锅仔记忆、人数、预算、厨艺和计划约束，并校验步骤数、天数和忌口。
- [ ] 1.3 实现保存计划、查询当前计划、替换单日和将计划日晚餐预填到记录页的后端流程。
- [ ] 1.5 保留每次生成的历史计划，提供当前/历史查询、单计划读取及收藏状态更新，且全程按登录用户隔离。
- [ ] 1.6 为计划日复用 Agnes 图像服务按需生成并缓存菜品封面；图片失败回退而不阻断计划正文。
- [ ] 1.7 接入一次性“服务完成通知”：服务端安全获取并缓存 access token，在计划保存成功后按用户授权发送模板消息，失败不影响主流程。
- [ ] 1.4 为计划生成约束、鉴权和单日替换补充后端测试。

## 2. 买菜清单合并

- [ ] 2.1 实现食材名称基础规范化、分类与数量合并，生成肉蛋豆、蔬菜、主食和调料购物项。
- [ ] 2.2 实现购买勾选保存，以及单日替换后重新计算但保留未受影响购买状态的逻辑与测试。

## 3. 周计划前端体验

- [ ] 3.1 新增“我的”页“锅仔帮你安排这一周”入口和周计划器，收集最少条件并展示生成进度、失败重试和价格说明。
- [ ] 3.4 重做周计划入口：已有计划时显示当前计划、重新生成和历史入口；新增可收藏、可打开的计划历史列表。
- [ ] 3.5 为七日食谱卡新增封面、生成中占位与图片失败回退，不在前端暴露模型密钥。
- [ ] 3.6 仅在用户点击生成时请求订阅授权，并将授权结果安全传递到计划完成通知流程；非微信端正常降级。
- [ ] 3.2 新增计划详情页：以一周七张左右滑动的食谱卡聚焦单日，支持周几直接切换；按需展示食材和五步做法，并提供食材复用提示、健康搭配、单日换菜与完成记录入口。
- [ ] 3.3 新增可折叠的分组买菜清单、勾选交互和计划保存/恢复状态，适配深色模式与小程序安全区。

## 4. 验证

- [ ] 4.1 验证生成、保存、重开、换菜、购物勾选和记录预填的完整闭环。
- [ ] 4.2 运行后端测试、前端静态检查、跨端构建检查及 OpenSpec 严格校验。

## 5. 每日菜品组合

- [x] 5.1 在计划器中提供每天 1–3 道菜选择，并为三人默认两道菜
- [x] 5.2 扩展生成契约与计划日数据，使新计划按每天菜品组合生成并正确合并买菜清单，同时兼容旧计划
- [x] 5.3 在按天食谱卡中展示当天菜品组合、各菜食材和做法，并保留换一天与记录入口
- [ ] 5.4 验证两道菜生成、旧计划读取和前后端静态检查

## 6. 锅仔周计划智能体

- [x] 6.1 让锅仔智能体按用户记忆、忌口、反馈、健康目标和去重策略规划每日菜品组合，并提供本地回退
- [x] 6.2 将周计划生成与单天换菜接入智能体，确保买菜清单仍按最终菜品组计算
- [ ] 6.3 验证智能体规划不重复、忌口过滤与前后端检查

## 7. 周计划封面与分享性能

- [x] 7.1 计划日先显示已有菜谱图或占位、后台生成 AI 封面；服务端按主菜名复用已生成的封面 URL
- [x] 7.2 在每张计划日卡片提供可保存的宣传型食谱卡，复用 Canvas 导出图片、材料清单和做法
- [ ] 7.3 验证保存卡片、封面失败回退及前端静态检查

## 8. 可选做饭日期

- [x] 8.1 默认选中周一至周日，并允许用户排除有事不做饭的星期
- [x] 8.2 将选中星期传至服务端，按实际星期保存计划，替换菜时保留原日期标签
- [ ] 8.3 验证非连续日期计划的生成、详情展示与前后端静态检查

## 9. 多菜封面轮播

- [x] 9.1 将计划日封面改为每道菜独立保存和按需生成，兼容旧计划主菜封面
- [x] 9.2 在日卡内增加多道菜封面轮播和菜名切换，导出当前菜的食谱卡
- [ ] 9.3 验证两道菜的独立封面生成、轮播、分享和前后端静态检查
