## 1. 隐私与真实进度

- [x] 1.1 删除模型推理正文及用户画像相关日志，只保留非敏感运行指标
- [x] 1.2 移除非流式文本调用的 THINKING 事件并更新进度测试

## 2. 饮食安全与记忆可靠性

- [x] 2.1 复用现有偏好字段实现统一的菜名和配料限制校验，并接入普通与深度 AI 推荐
- [x] 2.2 为记录日期增加严格请求校验，并让历史日期解析安全跳过脏数据
- [x] 2.3 将记忆快照改为只读，在首页寄语入口记录打开事件，并修正时间窗口与新用户判断

## 3. AI 推荐反馈闭环

- [x] 3.1 新增不保存菜谱正文的推荐曝光实体、仓库和最小查询能力
- [x] 3.2 为成功 AI 推荐生成曝光标识，并将近期展示和拒绝结果用于后续去重
- [x] 3.3 扩展反馈 API 与前端类型，使 AI 曝光支持喜欢、不想吃和做过，同时保持本地菜谱兼容

## 4. 付费权益与代码清理

- [x] 4.1 在消费权益前验证深度推荐请求，并实现异常与空结果的单次权益补偿
- [x] 4.2 删除推荐控制器中已被 GuozaiAgent 替代的重复私有代码

## 5. 验证

- [x] 5.1 补充 AI 忌口拒绝、非法历史日期、曝光反馈与权益恢复测试
- [x] 5.2 运行后端定向及完整测试、前端类型检查，并执行 OpenSpec 严格验证
