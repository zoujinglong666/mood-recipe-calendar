## 1. 可靠菜谱底座

- [x] 1.1 将现有内置菜谱迁移为版本化资源目录，补齐到至少 200 道唯一菜谱，并为每道填写具体食材、可执行步骤、菜系/风味、心情、时长、难度、简介和稳定图片兜底
- [x] 1.2 改造启动同步为按规范化菜名幂等插入或更新内置占位内容，验证旧菜谱主键及曝光、反馈、记录关联不变
- [x] 1.3 添加一个目录质量测试，覆盖 200+ 数量、唯一性、必填字段、食材与步骤 JSON、占位词和时长范围

## 2. 锅仔记忆与推荐学习

- [x] 2.1 扩展现有记忆响应，分别返回显式偏好和由近期反馈/记录聚合的可解释摘要，并保持清除记忆时保留用户做菜记录
- [x] 2.2 在锅仔记忆页复用现有编辑表单增加“锅仔这样认识你”摘要、数据来源、空状态、清除确认、401 恢复和深色模式检查
- [x] 2.3 收敛 GuozaiAgent 推荐顺序为硬性忌口过滤、拒绝与近期曝光排除、正向偏好加权、AI 选择/理由、本地降级，并添加推荐不重复及不违反忌口的后端测试
- [x] 2.4 让喜欢/不想吃反馈按用户与本次推荐幂等保存并可回读当前状态；将“做过”改为打开记录草稿，只有记录成功时写入一次 MADE

## 3. 今日推荐与做菜模式

- [x] 3.1 补齐今日推荐 API 类型和页面状态，使推荐结果展示真实工具阶段、个性化理由、三类反馈当前态、开始做主按钮及失败重试，不重写现有分享卡
- [x] 3.2 新增锅仔 IP 延展的做菜模式页面和路由，完成逐步卡片、食材折叠、上一/下一步、88rpx 触控区、深浅主题与无步骤拦截
- [x] 3.3 用 uni-app 本地存储实现单计时器开始/暂停/继续/重置、时间戳恢复、步骤进度恢复和重新开始；微信端轻震动，H5 无阻断降级
- [x] 3.4 从今日推荐和历史详情复用同一个做菜草稿入口，最后一步把完整来源信息预填到记录页并清除已完成的做菜进度

## 4. 一键记录与时光机回流

- [x] 4.1 为记录提交增加客户端提交标识和服务端幂等约束，使记录创建与 MADE 反馈在同一事务中只执行一次，并保留旧客户端兼容
- [x] 4.2 调整记录页：推荐来源允许使用推荐图或默认图直接保存，手动入口保持清楚的必填提示，失败保留草稿，401 登录后可继续
- [x] 4.3 保存成功后进入对应时光机记录详情；为有关联菜谱的历史记录增加“查看菜谱”和“再做一次”，无关联记录隐藏无效入口

## 5. 全链路验证

- [x] 5.1 运行后端测试，分别验证空库初始化、旧库幂等升级、反馈幂等、记录重试和 AI 失败时本地推荐
- [x] 5.2 运行前端类型检查、H5 构建和微信小程序构建，修复本变更新增错误且不覆盖仓库既有无关改动
- [ ] 5.3 手工走通未登录与已登录的“选心情 → 推荐 → 换一道/反馈 → 做菜并恢复 → 无照片记录 → 时光机回看 → 再做一次”，检查加载、空、失败、重试、深色模式和 H5/微信差异
